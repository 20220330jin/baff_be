spring:
  profiles:
    active: local

# 계정 통합 API (기본 비활성화 -- Phase 2 FE + CP2-FE + 내부 테스트 + 릴리즈 승인 후 true로 전환)
# Plan v3 Task 1.5-9, Plan Review Round 3 P0
baff:
  account-link:
    enabled: false

# 리워드/환전 기능 (기본 비활성화 -- 운영 무영향)
changeup:
  reward:
    enabled: false

reward:
  dummy-mode: true

# AI 분석 (기본 비활성 -- application-local.yml에서 키 설정)
ai:
  claude:
    api-key: ""
    base-url: https://api.anthropic.com/v1
    model-haiku: claude-haiku-4-5-20251001
    model-sonnet: claude-sonnet-4-5-20241022